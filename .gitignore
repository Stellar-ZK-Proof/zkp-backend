node_modules/
.env
dist/
.DS_Store
# Do NOT ignore circuit files - they are committed intentionally
# circuits/*.wasm  <- committed
# circuits/*.zkey  <- committed
# circuits/verification_key.json  <- committed
